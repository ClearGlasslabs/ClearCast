# Active Site

ClearGlassInc Artemis active domain: **https://clearglassinc.io**

Optional verification (Python):
```python
import requests
print(requests.get("https://clearglassinc.io", timeout=10).status_code)
```
