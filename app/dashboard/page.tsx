import { LiveMetricCard, LiveSignalGrid } from "@/components/live";
export const metadata={title:"Authorized dashboard"}; export default function Dashboard(){ return <><section className="page-head"><p className="eyebrow">AUTHENTICATED WORKSPACE</p><h1>Private signals stay server-side.</h1><p>The dashboard stream requires verified identity, workspace context, an authorized role and tenant-scoped projection. No sample client metrics are shown.</p></section><LiveSignalGrid>{["Project activity","Deployment events","Performance metrics","Conversion events","Approval queue","Compliance tasks"].map(title=><LiveMetricCard key={title} title={title} disabled freshness={{state:"unavailable"}}/>)}</LiveSignalGrid></>}
