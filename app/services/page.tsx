import { LiveMetricCard } from "@/components/live"; import { getSnapshot } from "@/lib/live/snapshot";
export const metadata = { title: "Services" };
export default async function Services(){ const s=await getSnapshot("status"); return <><section className="page-head"><p className="eyebrow">SECURE SYSTEMS ENGINEERING</p><h1>Mission systems designed for evidence.</h1></section><div className="cards"><article className="glass"><h2>Live data fabric</h2><p>Provider-neutral ingestion, validated envelopes, replay-safe delivery and privacy-aware projection.</p></article><article className="glass"><h2>AI governance</h2><p>Human-approved improvement proposals, evaluation gates, immutable provenance and rollback.</p></article><article className="glass"><h2>Cloud assurance</h2><p>Cloud Run delivery, zero-trust access, OpenTelemetry and production activation controls.</p></article></div><LiveMetricCard title="Current delivery availability" disabled={!s.enabled} freshness={s.freshness}/></>;}
