import { LiveActivityTimeline, LiveMetricCard, LiveSignalGrid, LiveSystemMap } from "@/components/live";
import { getSnapshot } from "@/lib/live/snapshot";
export default async function Home() { const snapshot = await getSnapshot("public"); return <><section className="hero glass"><p className="eyebrow">CLEARGLASS LIVE SIGNAL FABRIC</p><h1>Verified signals.<br/><span>Operational clarity.</span></h1><p>Server-rendered intelligence stays useful without a stream. Live labels appear only after a configured, validated source publishes a fresh event.</p><div className="actions"><a className="button" href="/status">Inspect platform status</a><a href="/services">Explore capabilities →</a></div></section><section><div className="section-title"><div><p className="eyebrow">PUBLIC SIGNAL PLANE</p><h2>Trust starts with provenance.</h2></div><p>There are no decorative counters here. Unconfigured sources are disclosed rather than simulated.</p></div><LiveSignalGrid><LiveMetricCard title="Platform availability" disabled={!snapshot.enabled} empty freshness={snapshot.freshness} accessibilityLabel="Platform availability source status" /><LiveSystemMap disabled={!snapshot.enabled} freshness={snapshot.freshness} /><LiveActivityTimeline updates={snapshot.events} freshness={snapshot.freshness} /></LiveSignalGrid></section><section className="glass prose"><p className="eyebrow">CONTROL PRINCIPLE</p><h2>Static first. Live when verified.</h2><p>The initial snapshot is rendered on the server, optional SSE begins after consent and hydration, polling preserves continuity, and one failed signal can never blank a page.</p></section></>; }
